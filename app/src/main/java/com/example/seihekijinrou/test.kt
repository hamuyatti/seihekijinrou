package com.example.seihekijinrou

/*import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import androidx.preference.PreferenceManager
import com.example.seihekijinrou.MeetingandVoting.OnlyFirstMeetingtime
import com.example.seihekijinrou.Preparation.Result2
import com.example.seihekijinrou.databinding.ActivityTestBinding

class test : AppCompatActivity() {
    private lateinit var binding :ActivityTestBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        binding = ActivityTestBinding.inflate(layoutInflater)
        super.onCreate(savedInstanceState)
        setContentView(binding.root)

        var pref = PreferenceManager.getDefaultSharedPreferences(this)
        var test = pref.getString("name1","")

        binding.textView8.text = test

        binding.button6.setOnClickListener {
            val intent = Intent(this, ::class.java)
            startActivity(intent)
        }
    }
}*/